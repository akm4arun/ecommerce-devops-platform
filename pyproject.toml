[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ecommerce-devops-platform"
version = "0.1.0"
description = "Enterprise-style e-commerce DevOps platform"
requires-python = ">=3.11"
dependencies = [
    "Flask>=3.1,<4",
]

[tool.setuptools.packages.find]
include = ["app*"]